# Enterprise license — bring your own, never commit
license.pem

# Generated PKI (demo CA, keys, leaf certs)
demo-pki/out/

# k3s runtime kubeconfig
k3s/kubeconfig/

# Local certificate working files — never commit private keys
*.key
*.csr
chain.crt
teleport-csr.cnf
